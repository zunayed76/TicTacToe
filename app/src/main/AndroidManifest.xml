<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_64"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_64"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication" >

        <activity android:name=".mainpage" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".human"></activity>
        <activity android:name=".easy" />
        <activity android:name=".hardcore" />
        <activity android:name=".normal" />


    </application>





</manifest>